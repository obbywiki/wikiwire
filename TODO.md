# TODO

* alternative login options(?)
* official support for non-github runners (maybe bitbucket support too)
* make outputs prettier (visually group cascading outputs)
* if an upload error is non-terminative, continue other uploads in background while continuing exp. backoff