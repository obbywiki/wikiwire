# TODO

* alternative login options(?)
* official support for non-github runners (maybe bitbucket support too)
* partial failure (continue remaining jobs after a permanent per-page error)
* (if provided) option to display which user pushed the change and a 7 letter commit sha if available